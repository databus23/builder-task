resources:
  - name: test.git
    type: git
    source:
      uri: https://github.com/databus23/builder-task.git
      branch: test
jobs:
  - name: nase
    plan:
      - get: test.git
      - task: build
        privileged: true
        file: test.git/ci/build.yaml
        input_mapping: {builder: test.git}
      - task: run
        image: image
        config:
          platform: linux
          run:
            path: sh
            args:
            - -c
            - |
              echo hello from my image I just build
              echo is it really my container: $IS_IT_MY_IMAGE

